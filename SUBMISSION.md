# Atoms Demo · 提交说明 / Submission Notes

> **面向评审的一页纸总结**，对应笔试文档要求的三点核心：
> ① **实现思路与关键取舍**　② **当前完成程度**（做了 / 没做 / 为什么）　③ **扩展路线与优先级**。
>
> 📖 **深度阅读导航**
> - 架构与设计细节（含数据模型、API 契约、安全策略）→ [DESIGN.md](./DESIGN.md)
> - 运行指南、技术栈、测试覆盖 → [README.md](./README.md)

[![Live Demo](https://img.shields.io/badge/🌐-在线体验-brightgreen?style=for-the-badge)](https://atoms-demo-lted.onrender.com)
[![Source](https://img.shields.io/badge/GitHub-源码-181717?style=for-the-badge&logo=github)](https://github.com/eviessun/atoms-demo)

---

## TL;DR

**Atoms Demo** 是一个受 Atoms / v0 / bolt.new 启发的**迷你 AI 应用生成器**。
用自然语言描述需求 → LLM 生成自包含单文件 HTML → 沙箱 iframe 实时预览 → 登录后可继续对话、**原地迭代**当前应用。

全链路真实可交互、数据持久化到云数据库、支持多模型切换与多模态输入（图片/语音），并在免费云服务上跑通了完整部署。

| 笔试题面硬性要求 | 落地情况 |
|---|---|
| ✅ 可运行、智能体驱动生成 + 可视化展示 | 生成/迭代闭环 + 沙箱 iframe 实时渲染 + SSE 流式输出 |
| ✅ **真实交互**（非静态页面） | 生成后可继续对话，模型在当前 HTML 上原地修改 |
| ✅ **数据持久化** | 线上 Neon PostgreSQL / 本地 SQLite，跨重启/重新部署保存 |
| ✅ **可测试的在线链接** | https://atoms-demo-lted.onrender.com |
| ✅ **公开代码仓库** | https://github.com/eviessun/atoms-demo |
| ✅ 基本流程（注册/登录/核心主流程） | 邮箱注册登录 → 生成 → 保存到"我的项目" → 迭代循环 |
| ✅ 至少一个延展/衍生能力 | 见下方"额外能力"清单（共 10+ 项） |

---

## 一、实现思路与关键取舍

### 核心架构思路

一句话描述：**"生成 → 预览 → 保存 → 迭代"的 Agent 闭环**。
区别于一次性 HTML 生成器，本项目的关键设计是让 LLM **在现有 HTML 的基础上持续修改**，让用户能像和设计师沟通一样"改改这里、加个那个"。

```
用户输入 → 后端拼装 prompt（含当前 HTML + 修改指令）→ LLM 返回完整 HTML
    → 沙箱 iframe 渲染 → 保存为新版本 → 用户继续提需求……
```

### 七个关键技术取舍

详细推导见 [DESIGN.md §3 关键设计取舍](./DESIGN.md)，这里只点题：

| 取舍点 | 选择 | 为什么 |
|---|---|---|
| **LLM 接入** | 模型注册表 + 可替换传输层，不写死 provider | 加模型 = 一行配置 + 一个环境变量，复用同一段 OpenAI 兼容传输代码；**API key 永不下发到浏览器** |
| **零 Key 可用性** | 内置 keyless `mock` 模型 | 无任何 key 也能跑通全链路、演示迭代；线上模型出错时可优雅降级 |
| **迭代的基准 HTML** | **服务端按 user_id 取**，客户端无法伪造 | 防越权/伪造基准；避免项目列表堆积重复条目 |
| **持久化** | `db.py` 双后端：Postgres（prod）/ SQLite（local），一套接口 | 本地零配置、线上跨部署保存；SQL 占位符自动转换、INSERT id 桥接 |
| **认证** | 标准库 PBKDF2-HMAC-SHA256 + httponly cookie | 避免 bcrypt/argon2 在 Render 上编译原生扩展失败；`Secure` 标记读 `X-Forwarded-Proto` 自适应 |
| **预览安全** | iframe `sandbox="allow-scripts allow-forms allow-modals"` | 允许生成的应用自跑脚本/表单，但**无同源权限**，不能访问父页面 DOM/cookie |
| **技术栈刻意从简** | 前端无构建、后端仅 FastAPI + psycopg、Python 锁 3.12 | 把时间预算砸在"闭环可用 + 可部署 + 可扩展"，而不是脚手架；避免 Python 3.14 无 wheel 的编译坑 |

---

## 二、当前完成程度

### ✅ 已完成

**硬性要求全部落地**（见上方 TL;DR 表格），额外实现了以下延展能力：

- **SSE 流式生成** — 实时展示推理过程与代码逐字输出，体验近同 Cursor/Trae
- **版本历史 + 非破坏性回滚** — append-only `project_versions` 快照表；回滚本身也记为新版本，历史永不截断
- **多模型下拉 + BYOK** — Trae 风格模型选择器；下拉只展示已配 key 的模型；支持用户自备 Key
- **优雅降级** — 选中模型出错（key 失效/额度/网络）自动降级到 mock，不硬失败
- **导出能力** — 下载单文件 `index.html`、复制源码、新标签页独立运行（脱离沙箱）
- **服务端幂等创建** — 每个创建请求带 idempotency key，重试/重放不会产生重复项目
- **一键中/EN 双语** — `i18n.js` 运行时替换，偏好存 localStorage，动态内容监听 `langchange` 重渲染
- **输入工程细节** — Enter 发送 / Shift+Enter 换行、输入法合成保护
- **多模态输入**：
  - 图片附件：base64 data URL；前后端**双重门控**（只对 vision 模型开放）；服务端清洗 + 张数/大小限制
  - 语音转写：浏览器 Web Speech API，**零后端、零成本、零 key**；不支持时自动隐藏麦克风
- **精选作品画廊** — 无需登录即可预览预置的两个示范应用（番茄钟 / 城市漫游）；未登录用户打开精选作品时提示登录
- **时间展示** — 项目/版本时间戳统一按北京时间（UTC+8）`YYYY-MM-DD HH:MM:SS` 展示
- **离线测试套件**：
  - 后端：`pytest` 覆盖认证 · 生成 · 归属隔离 · 版本 · 幂等 · 图片（纯 mock + 临时 SQLite，不碰真实 DB/网络）
  - 前端：零依赖 `node --test` 覆盖图片上传 · 语音 · 预览刷新（无 jsdom、无 package.json，直接跑真实源码）

### ⚠️ 有意未做（及理由）

| 未做项 | 理由 |
|---|---|
| 生成产物的**移动优先**约束 / 多视口预览 | 产品本身已做响应式；生成 HTML 的移动端约束属于提示词工程，作为 P0 扩展 |
| **付费/商业化**（套餐、计费、支付） | 需要用量计量基建作为前置，作为 P1 扩展 |
| **多文件工程生成**（HTML/CSS/JS 拆分、依赖安装） | 单文件 HTML 已足够支撑笔试演示；多文件需要文件树视图与沙箱加载器，作为 P2 扩展 |
| **协作/分享**（公开链接、团队空间） | 属于生态能力，PMF 验证后再做，作为 P3 |
| **第三方登录**（Google/GitHub）/ 邮箱验证 / 找回密码 | 邮箱密码已满足"注册登录"硬要求，第三方登录属加分项 |
| Render 免费档**冷启动**延迟（~30-60s） | 已配 10 分钟保活 ping 缓解；是平台限制，彻底解决需升级付费 |

---

## 三、若继续投入：扩展方向与优先级

按"**用户价值 / 实现成本**"从高到低排列，每级都附带触发信号与核心工作项。

### P0 — 多端可用（移动端 + PC 端）

**目标**：产品本身 + 生成出来的应用都能在手机/平板/PC 顺畅使用。
- 生成侧：在 system prompt 里强化"mobile-first、响应式"约束；预览面板增加"📱手机 / 📋平板 / 🖥️桌面"视口切换
- 产品侧：三栏布局在移动端收敛为可切换的 Tab/抽屉；触控目标≥44px
- **为什么最高优先**：直接影响"第一印象能不能点两下就跑通"，且主要是 CSS + prompt engineering，成本可控

### P1 — 付费 / 商业化能力

**目标**：建立可持续的商业模式。
- 分层套餐（免费额度 + 付费更高额度/更强模型），按 token/次/月计费
- **前置基建**：用量计量中间件（每次生成记录 token 消耗/模型/时长）——计量是计费的基础
- 接入支付（Stripe），付费用户解锁 GPT-4o/Claude 等高级模型、更长版本历史、工程导出
- **触发信号**：日均生成次数 > 免费模型额度上限；用户主动询问"能不能用 Claude"

### P2 — 生成能力增强

- 从单文件 HTML 扩展到**多文件工程**（HTML/CSS/JS 分离 + 依赖声明），配合文件树视图与独立加载器
- **运行时错误回填自愈**：捕获沙箱内的 console.error / runtime exception，自动回传给模型修复
- **视觉稿到代码**：上传截图/手绘稿 → 生成对应 UI（结合多模态能力）

### P3 — 协作与生态

- 项目**公开分享链接**（只读预览 + fork）、团队空间、评论批注
- 第三方登录（Google/GitHub）、邮箱验证与密码找回
- **精选社区**：用户提交作品 → 审核 → 入选画廊（复用现有 featured 机制，扩展为 UGC）

### 优先级判断依据

```
P0 多端 ─── 体验门槛最低、ROI 最高，把 Demo 变"真能用"
   │
P1 付费 ─── 决定能不能活，但依赖计量基建
   │
P2 能力 ─── 纵深护城河，成本高但差异化强
   │
P3 生态 ─── 网络效应，PMF 验证后再投
```

---

## 四、规模化与成本：从免费档到付费档的演进路径

当前部署（Neon 免费档 + OpenRouter 免费模型）足够支撑 Demo 与评审体验。但数据量/并发上升时，**瓶颈会先出在代码上，必须先做代码侧优化，再按可观测指标分阶段升级付费资源**。

### 阶段 0 — 代码侧优化（先于任何付费升级，低成本必做）

| 问题 | 优化 |
|---|---|
| 版本快照存**全量 HTML**，线性膨胀 | 存 diff 而非全量；或只保留最近 N 版 + 冷数据归档到对象存储 |
| 列表查询**无分页**，一次捞全量 | `LIMIT/OFFSET` 或游标分页 |
| **每请求新建连接**，无连接池 | 引入 `psycopg_pool`；补索引 `projects(user_id)`、`sessions(user_id)` |

### 阶段 1 — 数据库升级（当免费档真的扛不住时）

- **触发信号**（用指标而非拍脑袋）：存储逼近 Neon 免费上限、连接数频繁打满、P95 查询延迟上升
- 纵向扩容 → Neon 付费档（更大存储、更多连接、autoscaling、读副本）
- **冷热分离**：`projects.html`（热，当前版本）留主库；`project_versions` 历史 HTML（冷）迁到 S3/R2，DB 只存指针
- 读写分离：列表/预览走只读副本

### 阶段 2 — 模型升级（与 P1 付费互补）

- **智能路由**：简单需求走免费/小模型，复杂需求自动升级到付费强模型（GPT-4o/Claude）
- **结果缓存**：相同/相似 prompt 命中缓存，省 token 秒出
- 付费用户解锁高级模型、更长版本历史 —— **用量计量是这一切的前置**

### 阶段 3 — 规模化基建

- 读缓存（Redis）、限流防刷、生成任务队列（异步排队削峰）、可观测性（慢查询/连接数/DB 体积监控告警）

---

## 五、快速验收指引

建议评审按此顺序体验，3–5 分钟可走通核心闭环：

1. **打开** [在线 Demo](https://atoms-demo-lted.onrender.com) — 首屏中文，右上角"🌐 中 / 模型下拉 / 登录"
2. **切换语言** — 点 `🌐 中` → 全站变英文；刷新后保持
3. **注册/登录** — 点"登录/注册" → 独立登录页（tab 切换、密码显隐、友好错误）→ 注册后回主页
4. **生成** — 输入"一个带开始/暂停的番茄钟" → 右侧 iframe 实时渲染，左侧"我的项目"+1
5. **迭代（Agent 核心）** — 再输入"加一个任务清单" → **同一应用原地更新**，不新增项目
6. **持久化验证** — 退出再登录，项目仍在（Neon Postgres）
7. **额外探索** — 试语音按钮、图片上传（选有 vision 标识的模型）、版本历史回滚、导出 HTML

> ⚠️ 首次访问若实例在休眠，需等 ~30–60s 冷启动，属 Render 免费档正常现象。
