# 提交说明 / Submission Notes — Atoms Demo

> 面向评审的一页纸说明，对应笔试文档「强烈建议附带一份简要说明文档」的三点要求：
> **① 实现思路与关键取舍 ② 当前完成程度（做了 / 没做） ③ 若继续投入会如何扩展及优先级**。
> 更详细的架构与设计在 [DESIGN.md](./DESIGN.md)，运行方式在 [README.md](./README.md)（[中文](./README.zh-CN.md)）。

- 在线体验 / Live demo: https://atoms-demo-lted.onrender.com
- 源码 / Source: https://github.com/eviessun/atoms-demo

---

## 一、实现思路与关键取舍

**一句话**：用自然语言描述一个网页应用 → 后端交给 LLM 生成**自包含单文件 HTML** → 前端在**沙箱 iframe** 里实时渲染 → 登录用户可继续对话，让模型在当前 HTML 上**原地迭代**。这条"生成 → 预览 → 保存 → 迭代"闭环，是它区别于一次性生成器、更像 Agent 的地方。

关键取舍（详见 DESIGN.md 第 3 节）：

- **模型层用注册表 + 可替换传输，而非写死某个 provider**。加一个模型 = 加一行注册表 + 配一个 key，其余代码不动；DeepSeek / 豆包 / Kimi / OpenRouter / OpenAI 都是 OpenAI 兼容，复用同一段传输。**API key 只在服务端读取，浏览器只发 model id**，避免前端泄露。
- **内置 keyless `mock` 模型**：无任何 key 也能演示全链路，让"先部署上线、再接真实模型"成为可能，部署不被 key 卡住；线上模型出错时还能降级到 mock，演示不硬失败。
- **迭代时基准 HTML 由服务端按 `user_id` 取**，客户端无法伪造，既防越权也避免项目列表堆重复。
- **持久化双后端一套接口**：本地零配置用 SQLite，线上用 Neon Postgres 跨重新部署保存数据——这是"数据持久化"硬指标的关键。
- **认证只用标准库**（PBKDF2 + httponly cookie），避免在 Render 现场编译原生扩展导致部署失败。
- **技术栈刻意从简**：前端无构建步骤，后端只依赖 FastAPI + psycopg，Python 锁 3.12 直接用 wheel。取舍是把 6–8h 的预算投在"闭环可用 + 可部署 + 可扩展"上，而不是脚手架。

---

## 二、当前完成程度

### ✅ 已完成（对照笔试硬性要求）

| 笔试要求 | 落地情况 |
| --- | --- |
| 可运行、智能体驱动生成 + 可视化展示 | 生成/迭代闭环 + 沙箱 iframe 实时预览 |
| **真实交互（非纯静态）** | 生成后可继续对话，原地迭代当前应用 |
| **数据持久化** | 线上 Neon Postgres / 本地 SQLite，跨重启保存 |
| **可测试的在线链接** | https://atoms-demo-lted.onrender.com |
| **代码链接（public）** | https://github.com/eviessun/atoms-demo |
| 基本流程（注册 / 核心主流程） | 邮箱注册登录 → 生成 → 保存 → "我的项目" |
| 至少一个延展能力 | 见下方"额外能力" |

**额外能力（延展加分项）**：

- 多模型下拉（key 安全）+ BYOK 自备 key + 选中模型出错时优雅降级
- **SSE 流式生成**：实时展示推理过程与代码逐字写出
- **版本历史 + 非破坏性回滚**：每次生成/迭代/回滚都记快照，回滚本身也记为新版本
- 生成应用可**导出**：下载单文件 HTML / 复制源码 / 新标签页独立运行
- 服务端**幂等创建**：重试/重放不会产生重复项目
- 一键中 / EN 双语，偏好记忆
- 输入框 **Enter 发送 / Shift+Enter 换行**（含输入法合成保护）
- **项目/版本时间戳按北京时间展示**（UTC+8，`YYYY-MM-DD HH:MM:SS`）
- 离线 `pytest` 测试套件：认证 · 生成 · 归属隔离 · 版本 · 幂等

### ⚠️ 尚未做 / 有意未做

- **移动端 / PC 端多端适配**：目前偏桌面三栏布局，未做响应式与移动端专门优化（见扩展第 1 项）。
- **付费 / 商业化能力**：无套餐、额度、计费、支付（见扩展第 2 项）。
- **生成产物仅单文件 HTML**：不支持多文件工程、依赖安装、后端代码生成。
- **协作与分享**：项目仅归属本人，无公开分享链接、无团队协作。
- **无第三方登录**（仅邮箱 + 密码），无邮箱验证 / 找回密码。
- **免费档限制**：Render 空闲会休眠，首次访问需 ~30–60s 冷启动（已用保活 ping 缓解）。

---

## 三、若继续投入：扩展方向与优先级

按"用户价值 / 实现成本"排序，优先级从高到低：

### P0 — 多端可用（移动端 + PC 端）
让**生成出来的应用**和**本产品本身**都能在移动端与 PC 端顺畅使用。
- 生成侧：在系统提示里强化"移动优先、响应式"约束，让产出的 HTML 默认自适应；预览面板增加"手机 / 平板 / 桌面"多视口切换，一键预览不同尺寸。
- 产品侧：三栏布局做响应式收敛（移动端改为可切换的抽屉 / 标签页），触控友好。
- **为什么最高优先**：直接影响"可用、可体验"的第一印象，且成本相对可控（主要是 CSS 与提示词工程）。

### P1 — 付费 / 商业化能力
- 分层套餐（免费额度 + 付费更高额度 / 更强模型），按次或按月计费。
- 用量计量与配额（记录每次生成的 token / 次数，达到上限提示升级）。
- 接入支付（如 Stripe），付费用户解锁高级模型（GPT/Claude）、更多版本历史、导出为工程等。
- **前置**：先补齐用量计量的数据表与中间件，再叠加计费——计量是计费的基础。

### P2 — 生成能力增强
- 从"单文件 HTML"扩展到**多文件工程**（拆分 HTML/CSS/JS，甚至带简单后端），配合更强的代码视图与文件树。
- 生成结果的自动校验 / 运行时报错回填给模型自愈。

### P3 — 协作与分享
- 项目**公开分享链接**（只读预览）、团队空间、评论。
- 第三方登录（Google / GitHub）、邮箱验证与找回密码。

### 优先级判断依据
- **P0 多端**：投入产出比最高，直接提升"用户体验"评估维度，是把 Demo 变"真能用"的关键一步。
- **P1 付费**：决定能否商业化，但依赖计量基建，故排在多端之后。
- **P2/P3**：属于能力纵深与生态，价值大但成本高，适合验证 PMF 后再投入。

---

## 四、规模化与成本：从免费档到付费档的演进

当前部署所用的数据库（Neon 免费档）与模型（OpenRouter 免费档）足够支撑 Demo 与评审体验。但随着**数据量与并发上升**，需要演进到更高性能的付费部分。这里的关键判断是：**直接买更高档的 DB 并不能解决问题——瓶颈会先出在代码上，必须先做代码侧优化，再按可观测的指标分阶段升级付费资源。**

### 现状下随数据量增长会先暴露的瓶颈（基于当前代码）

- **版本快照存全量 HTML**：`project_versions` 表每次生成/迭代/回滚都追加一整份 `html`。一个项目改 N 次就存 N 份完整 HTML —— 这是**数据库体积膨胀的主因**，存储随迭代次数线性猛涨。
- **列表查询无分页**：`list_projects` / `list_versions` 只有 `ORDER BY id DESC`、没有 `LIMIT`，一次性把某用户的全部项目/版本捞出。数据一多，接口变慢、响应体变大。
- **每请求新建连接、无连接池**：`get_conn()` 每次新建连接，并发一高会打满 Neon 免费档的连接数上限。索引也仅有 `idx_pv_project`，`projects(user_id)`、`sessions(user_id)` 尚无索引。

### 分阶段演进路线

**阶段 0 — 先做"免费档也扛得住"的代码优化（低成本、必做，先于任何付费升级）**
- 列表接口加**分页**（`LIMIT/OFFSET` 或游标）—— 最立竿见影。
- 引入**连接池**（如 `psycopg_pool`），不再每请求建连。
- 补索引：`projects(user_id)`、`sessions(user_id)`。
- **版本快照瘦身**：存 diff 而非全量 HTML，或只保留最近 N 版 + 定期归档，直接砍掉最大的膨胀源。

**阶段 1 — 数据库升级（当免费档真的不够时）**
- **触发信号**（用指标而非拍脑袋决定）：存储逼近 Neon 免费档上限、连接数频繁打满、P95 查询延迟上升。
- **纵向扩容**：Neon 免费 → 付费档（更大存储、更多连接、autoscaling、读副本）。
- **冷热分离**：`projects.html`（热，当前版本）留主库；`project_versions` 的历史 HTML（冷）迁到**对象存储**（S3 / Cloudflare R2），DB 只存指针 —— 存储成本可降一个量级。
- **读写分离**：列表/预览走只读副本，减轻主库压力。

**阶段 2 — 模型升级（付费模型，与第三部分 P1 付费互补）**
- **智能路由**：简单需求走免费/小模型，复杂需求自动升级到付费强模型（GPT / Claude），平衡成本与质量。
- **结果缓存**：相同 prompt 命中缓存，省 token、秒出。
- 付费用户解锁高级模型、更长版本历史 —— **用量计量是这一切的前置**（也是计费基础）。

**阶段 3 — 规模化基建**
- 读缓存（Redis）、限流防刷、生成任务队列削峰（异步排队而非同步阻塞）、可观测性（慢查询 / 连接数 / DB 体积监控与告警）。

### 一句话总结
当前免费档够跑 Demo；数据量上来后，瓶颈会**先**出在无分页查询、全量快照存储和无连接池上，需要**先做代码侧优化，再按存储 / 连接 / 延迟等指标分阶段升级到付费 DB 与付费模型**。
