# Test / dev dependencies (not needed at runtime — kept out of requirements.txt
# so the Render build stays lean). Install with:  pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.3
